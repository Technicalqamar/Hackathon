// UI Module — UI سے متعلق functions (loader، toast، alert، confirm)
// یہ کسی بھی فائل پر منحصر نہیں
// app.js ان functions کو import کرے گا

const showLoader = (el) => {};
const hideLoader = (el) => {};
const showToast = (message, type) => {};
const showAlert = (message) => {};
const showConfirm = (message) => {};

export { showLoader, hideLoader, showToast, showAlert, showConfirm };
