// Cloudinary Module — اس فائل میں image upload کے functions ہوں گے
// یہ کسی بھی فائل پر منحصر نہیں، مکمل طور پر standalone ہے
// app.js ان functions کو import کرے گا

const uploadImage = (file) => {};
const deleteImage = (publicId) => {};
const getUrl = (publicId, options) => {};

export { uploadImage, deleteImage, getUrl };
